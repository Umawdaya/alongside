# 社區心衛中心地圖　PWA 部署說明

## 一、這個資料夾裡有什麼

| 檔案 | 用途 |
|---|---|
| `cmhc-map.html` | 地圖主頁面（74處據點資料全部內嵌其中） |
| `manifest.json` | App 設定檔（名稱、圖示、開啟方式） |
| `sw.js` | 離線快取程式（Service Worker） |
| `icon-192.png`／`icon-512.png` | Android 與桌面版圖示 |
| `icon-maskable-512.png` | Android 自適應圖示（會被裁成圓形或方形） |
| `apple-touch-icon.png` | iPhone／iPad 主畫面圖示 |
| `favicon-32.png` | 瀏覽器分頁小圖示 |

**八個檔案必須放在同一層**，缺一個就會有功能失效。

---

## 二、部署到 GitHub Pages

把整個資料夾的**八個檔案**上傳到 repo，放在同一層即可。以你現有的 `sann-ua-png` 為例：

```
sann-ua-png/
├── index.html              （試刊號）
├── issue-02.html           （第02期）
├── cmhc-map.html           ← 地圖（覆蓋舊版）
├── manifest.json           ← 新增
├── sw.js                   ← 新增
├── icon-192.png            ← 新增
├── icon-512.png            ← 新增
├── icon-maskable-512.png   ← 新增
├── apple-touch-icon.png    ← 新增
└── favicon-32.png          ← 新增
```

上傳後網址不變：`https://umawdaya.github.io/sann-ua-png/cmhc-map.html`

**重要：PWA 只在 HTTPS 環境下運作。** GitHub Pages 本身就是 HTTPS，所以沒問題；但如果你在電腦上用 `file://` 直接打開檔案，離線功能與安裝按鈕都不會出現，這是正常的，不是壞掉。

---

## 三、使用者會看到什麼

**Android／Chrome**
瀏覽網頁時，右下角會浮出「＋ 加到主畫面」按鈕，點了就能安裝。裝完後桌面出現獨立圖示，開啟時沒有網址列，跟一般 App 一樣。

**iPhone／Safari**
iOS 不支援自動安裝提示，需要手動操作：點下方分享鈕 → 選「加入主畫面」。圖示與名稱會自動帶入。

**電腦／Chrome、Edge**
網址列右側會出現安裝圖示，可安裝成獨立視窗的桌面應用程式。

---

## 四、離線能做什麼、不能做什麼

**可以（收訊不穩或完全沒網路時）**
- 查詢全部 74 處據點的名稱、地址、電話、傳真、服務時間、轄區
- 右側縣市清單的展開、搜尋、五區篩選
- 點電話直接撥號

**不行**
- **地圖底圖會空白**——圖磚是即時從伺服器抓的，無法預先快取全台灣所有縮放層級。不過**曾經瀏覽過的區域會被記住**，下次離線時那一帶仍看得到底圖（最多保留約 400 張圖磚）。
- 導航按鈕（需要連網開啟 Google 地圖）
- 各中心的官網與臉書連結

對訪視員在山區、離島查電話地址的情境，核心功能是完整的。

---

## 五、更新資料後怎麼辦

改完 `cmhc-map.html` 重新上傳即可。頁面本身採「網路優先」策略，使用者只要有網路，開啟時就會自動取得最新版，不需要重新安裝。

**若使用者反映看到的還是舊資料**：請他關閉再重開一次 App，或在瀏覽器重新整理。若要強制所有人立即更新，可以修改 `sw.js` 開頭的版本號：

```javascript
const VERSION = 'v1';   // 改成 'v2'
```

改版本號會讓舊快取全部失效、重新下載。

---

## 六、若要換圖示

圖示是深橄欖底（`#3a4a10`）配奶油米定位針（`#f7ede2`），沿用刊物色系。要換的話，準備同尺寸的 PNG 覆蓋即可：
- `icon-192.png`（192×192）
- `icon-512.png`（512×512）
- `icon-maskable-512.png`（512×512，圖案需集中在中央約 60% 範圍內，因為 Android 會裁邊）
- `apple-touch-icon.png`（180×180）

---

*《相倚傍報你知》｜能力有限功德無量發行*
